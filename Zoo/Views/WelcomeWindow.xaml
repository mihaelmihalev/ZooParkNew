<Window x:Class="Zoo.WelcomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Zoo"
        mc:Ignorable="d"
        Title="Избор" WindowStartupLocation="CenterScreen">
    <Window.Background>
        <ImageBrush ImageSource="C:\Users\mihael.mihalev\Desktop\Izpitvane\Zoo\Zoo\Images\background.jpg"></ImageBrush>
    </Window.Background>
    <Grid Margin="10,0,0,3">
        <Grid.RowDefinitions>
            <RowDefinition Height="0*"/>
            <RowDefinition Height="0*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Name="btnAnimals" Content="Животни" HorizontalAlignment="Left" Width="200" Height="100" VerticalAlignment="Top" Margin="20,7,0,0" Grid.Row="2"  Command="{Binding SelectAnimalsCommand}"></Button>
        <Button Name="btnEvents" Content="Събития" HorizontalAlignment="Center" Width="200" Height="100" VerticalAlignment="Top" Margin="0,7,0,0" Grid.Row="2"  Command="{Binding SelectEventsCommand}"></Button>
        <Button Name="btnTickets" Content="Билети" HorizontalAlignment="Right" Width="200" Height="100" VerticalAlignment="Top" Margin="0,7,20,0" Grid.Row="2"  Command="{Binding SelectedTicketsCommand}"></Button>
        <!--Grid Margin="0,0,0,389" Grid.RowSpan="3">
            <ListView Name="AnimalsView" ItemsSource="{Binding Animals}"  Margin="26,141,0,-329"   HorizontalAlignment="Center"  Width="474">
                <ListView.View>
                    <GridView>
                        <GridViewColumn  Header="Категория" Width="320"  />

                    </GridView>
                </ListView.View>
            </ListView>
        </Grid-->
        <!--Grid Margin="0,0,0,389" Grid.RowSpan="3">
            <ListView Name="EventsView" Margin="26,141,0,-329"   HorizontalAlignment="Center"  Width="474">
                <ListView.View  >
                    <GridView>
                        <GridViewColumn  Header="Събитие" Width="320"  />

                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
        <Grid Margin="0,0,0,389" Grid.RowSpan="3">
            <ListView Name="TicketsView" Margin="45,141,0,-329"   HorizontalAlignment="Center"  Width="676" >
                <ListView.View>
                    <GridView>
                        <GridViewColumn  Header="Категория" Width="320"  />
                    </GridView>
                    
                </ListView.View>
                
            </ListView>

        </Grid>
        <Button x:Name="btnSearch" Content="Търси" HorizontalAlignment="Right" Margin="0,0,20,15"  VerticalAlignment="Bottom" Width="90" Height="34" Grid.RowSpan="3"/>
        <Button x:Name="btnBuy" Content="Купи" HorizontalAlignment="Right" Margin="0,0,20,15"  VerticalAlignment="Bottom" Width="90" Height="34" Grid.RowSpan="3" />
        <Button x:Name="btnInfo" Content="Информация за събитието" HorizontalAlignment="Right" Margin="0,0,20,15"  VerticalAlignment="Bottom" Width="140" Height="34" Grid.RowSpan="3"  /-->

    </Grid>




</Window>
